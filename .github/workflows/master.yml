name: Master

on:
    push:
        branches:
            - master

jobs:
    build:
        runs-on: ubuntu-18.04
        steps:
            - name: Master build
              run: echo "This is the master build on ${GITHUB_REF}"